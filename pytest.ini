[pytest]
# The real test suite lives in tests/, not test/ - the latter is a folder
# of leftover asset dumps and dev notes (sprite sheets, an old coordinate
# log, a scratch text file) with no test code in it. Scoping discovery here
# keeps pytest from trying to import anything out of that folder.
testpaths = tests
