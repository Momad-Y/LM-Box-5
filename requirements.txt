# Directly imported by the app itself: cv2 (opencv-python), cvzone,
# mediapipe, numpy, pygame, platformdirs, screeninfo. pytest is a test-only
# dependency (see tests/). Everything else below is a real transitive
# dependency of one of those (mostly mediapipe, which pulls in a
# jax/matplotlib-based stack) - verified against each package's own
# installed metadata, not just guessed from familiarity.
#
# Previously this file also carried an entire unused Jupyter/IPython dev
# toolchain (ipykernel, jupyter_client, debugpy, tornado, traitlets, jedi,
# ipython, prompt_toolkit, etc.) and pygame-menu, left over from an
# unfiltered `pip freeze` - none of it is imported anywhere in this repo,
# and it would have bloated any executable built from this file.
absl-py==2.1.0
attrs==24.2.0
cvzone==1.6.1
cycler==0.12.1
flatbuffers==24.3.25
fonttools==4.55.0
kiwisolver==1.4.7
mediapipe>=0.8.9
numpy==1.26.4
opencv-python==4.10.0.84
opt_einsum==3.4.0
packaging==24.2
pillow==11.0.0
platformdirs==4.3.6
protobuf==4.25.5
pycparser==2.22
pygame==2.6.1
pyparsing==3.2.0
python-dateutil==2.9.0.post0
pytest==8.3.3
scipy==1.14.1
screeninfo==0.8.1
sentencepiece==0.2.0
six==1.16.0
typing_extensions==4.12.2
